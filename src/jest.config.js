module.exports = {
  testMatch: ['**/?(*.)+(test).[jt]s?(x)'], // Adjust to match your test file naming convention
  testPathIgnorePatterns: ['/node_modules/', '/e2e_tests/'], // Add any other directories to ignore
};
